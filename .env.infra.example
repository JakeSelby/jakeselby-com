# Copy to .env.infra (gitignored). Use shell-compatible KEY=value assignments.
# Optional when using another AWS credential source:
# AWS_PROFILE=your-profile
SITE_AWS_ACCOUNT_ID=
SITE_HOSTED_ZONE_ID=
SITE_BUCKET_NAME=
SITE_ROUTING_FUNCTION_NAME=
