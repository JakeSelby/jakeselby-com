# Google Analytics 4 measurement id, from Admin → Data streams → your web stream.
# Format: G-XXXXXXXXXX. Public by design — it ships in the page HTML.
# Only read during a production build; `astro dev` never reports traffic.
PUBLIC_GA_MEASUREMENT_ID=
